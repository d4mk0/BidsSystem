<%- model_class = Item -%>
<div class="page-header">
  <h1>Товары</h1>
  <%= link_to "Добавить новый товар",
              new_item_path,
              :class => 'btn btn-primary' %>
</div>
<div class="">
</div>
<div class="row">
  <% @items.each do |item| %>
    <div class="col-md-4">
      <div class="panel panel-default">
        <div class="panel-heading"><%= item.name %></div>
        <div class="panel-body">
          Создатель: <b><%= item.user.email %></b>
          <br>
          Стартовая цена: <b><%= item.start_sum %></b>
          <br>
          Минимальный шаг: <b><%= item.min_step %></b>
          <br>
          Текущая цена: <b><%= item.current_sum %></b>
          <%= simple_form_for :bid, html: { class: 'form-inline' } do |f| %>
            <%= f.input :stake, input_html: { value: item.min_stake }, label: false, required: false %>
            <%= f.button :submit, "Поставить" %>
          <% end %>
        </div>
      </div>
    </div>
  <% end %>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:start_sum) %></th>
      <th><%= model_class.human_attribute_name(:user_id) %></th>
      <th><%= model_class.human_attribute_name(:name) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @items.each do |item| %>
      <tr>
        <td><%= link_to item.id, item_path(item) %></td>
        <td><%= item.start_sum %></td>
        <td><%= item.user_id %></td>
        <td><%= item.name %></td>
        <td><%=l item.created_at %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_item_path(item), :class => 'btn btn-default btn-xs' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      item_path(item),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-xs btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

